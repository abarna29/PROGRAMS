#include <stdio.h>

int main()
{
    char s[100];
    int c=0,i=0;
    scanf("%s",s);
    while(s[i])
    {
        c++;
        if(s[i]!=s[i+1])
        {
            printf("%d%c",c,s[i]);
            c=0;
        }
        i++;
    }
	return 0;
}
